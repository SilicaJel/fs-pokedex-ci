name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello World!"
      - name : Print date 
        run: date
      - name: List directory contents (long format)
        run : ls -l